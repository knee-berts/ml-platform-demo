apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.inferencePoolName }}
  namespace: {{ .Values.inferenceNamespace }}
spec:
  selector:
    app: {{ .Values.inferencePoolName }}
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
  type: ClusterIP
