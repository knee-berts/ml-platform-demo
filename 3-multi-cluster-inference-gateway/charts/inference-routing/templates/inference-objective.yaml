# Production traffic — high priority, served first under contention.
# Clients set header: x-gateway-inference-objective: food-review-prod
apiVersion: inference.networking.x-k8s.io/v1alpha2
kind: InferenceObjective
metadata:
  name: food-review-prod
  namespace: {{ .Values.inferenceNamespace }}
spec:
  priority: 100
  poolRef:
    name: {{ .Values.inferencePoolName }}
    group: "inference.networking.k8s.io"
---
# Batch / experiment traffic — low priority, queued behind prod when saturated.
apiVersion: inference.networking.x-k8s.io/v1alpha2
kind: InferenceObjective
metadata:
  name: food-review-batch
  namespace: {{ .Values.inferenceNamespace }}
spec:
  priority: -10
  poolRef:
    name: {{ .Values.inferencePoolName }}
    group: "inference.networking.k8s.io"
