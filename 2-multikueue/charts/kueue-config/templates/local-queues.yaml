apiVersion: kueue.x-k8s.io/v1beta1
kind: LocalQueue
metadata:
  name: inference-queue
  namespace: {{ .Values.inferenceServerNamespace }}
spec:
  clusterQueue: gpu-cluster-queue
---
apiVersion: kueue.x-k8s.io/v1beta1
kind: LocalQueue
metadata:
  name: training-queue
  namespace: {{ .Values.trainingNamespace }}
spec:
  clusterQueue: gpu-cluster-queue
